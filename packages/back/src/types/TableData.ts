import { PlayerData } from "./PlayerData";

export type TableData = {
  players: PlayerData[];
  saved: boolean;
  expert: boolean;
  spiderWoman: number;
  superDamage: number;
  superThreat: number;
  ship: number;
  completeVeranke: boolean;
  enemy: number;
  exposed: number;
};
